# Personal Website (Flask + Frozen-Flask)

A single-page personal portfolio built with Flask and exported to static HTML for GitHub Pages.

## 🧱 Run Locally
```bash
pip install -r requirements.txt
flask run
